# Copy to .env (gitignored) and fill in as needed. The core path — validate, sweep,
# report, demo, retro, and the full pytest gate — needs NONE of these; they are only
# for the optional LLM narrative synthesis hook (report/synthesis.synthesize_narrative),
# which is not wired in the scaffold.

# API key for the eventual narrative-synthesis prose pass (deferred; no key assumed).
# ANTHROPIC_API_KEY=
